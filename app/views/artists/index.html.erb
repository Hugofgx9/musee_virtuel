<h1>Artistes</h1>


<div class="row1_artist no-gutters border_top">
    <% @artists.each do |artist| %>
      <div class="col-12 col-lg-3 col-md-6 border_right border_bottom change_backcolor">
          <%=link_to artist do %> <!-- ce link_to ne fonctionne pas -->
              <div><%= link_to artist.name, artist, class: "a_artistname change_textcolor" %> 
              </div>
          <% end %>
      </div>
    <% end %>
</div>
